{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "cd backend && uvicorn production_app:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
