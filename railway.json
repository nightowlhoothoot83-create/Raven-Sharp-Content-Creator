{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "cd backend && uvicorn app_v2:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120
  }
}
